import { GreyScaleDirective } from './grey-scale.directive';

describe('GreyScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new GreyScaleDirective();
    expect(directive).toBeTruthy();
  });
});
